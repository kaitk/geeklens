/** Settings snapshot shared by the badge leaf components and their mount adapter. */
export interface BadgePresentationPreferences {
  coloredBadges: boolean;
  tooltips: boolean;
  mappingWarnings: boolean;
}
